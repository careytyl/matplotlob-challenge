# Matplotlib

This Jupyter Notebook file uses Python to import multiple related sets of clinical and trial data, merges the information into a single dataframe, and creates individual dataframes. Numerous graphs are generated from the data to better visualize the effects that treatments provide.
